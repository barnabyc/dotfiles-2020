ripgrep
fd-find
fzf ( via `git clone --depth 1 https://github.com/junegunn/fzf.git ~/.fzf` )
vim
antibody ( via `curl -sfL git.io/antibody | sh -s - -b /usr/local/bin` )

# development
nvm

# work
rbenv
